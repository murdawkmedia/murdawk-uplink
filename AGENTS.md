# AGENTS.md

- Read `README.md` and `SECURITY.md` before changing transfer or credential code.
- Never commit credentials, connection packages, local manifests, logs, or user paths.
- Keep real uploads and remote mutations behind explicit user confirmation.
- Run `npm test` before committing and `npm run ui:smoke` before release.
- Use dry runs for transfer-command changes.
